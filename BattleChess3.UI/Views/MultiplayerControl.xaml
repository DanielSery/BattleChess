<UserControl x:Class="BattleChess3.UI.Views.MultiplayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:BattleChess3.UI.ViewModel"
             xmlns:converters="clr-namespace:BattleChess3.UI.Converters"
             xmlns:localization="clr-namespace:BattleChess3.UI.Localization"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewmodel:MultiplayerViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Column="0"
            Grid.Row="0" Grid.RowSpan="2"
            AllowDrop="True"
            Drop="Button_Drop"
            DragEnter="Button_DragEnter"
            Visibility="{Binding IsConnected, Converter={converters:BooleanMultiValueConverter TrueValue=Visible, FalseValue=Collapsed}}" >
            <TextBlock Text="{Binding GameId}"
                       Style="{DynamicResource TextBlockStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       MaxHeight="150" />
        </Border>

        <Button
            Grid.Column="1" Grid.Row="0"
            FontSize="25"
            Content="{localization:Loc MultiplayerControl_HostButton}"
            Command="{Binding HostAndCopyCommand}"
            Visibility="{Binding CanConnect, Converter={converters:BooleanMultiValueConverter TrueValue=Visible, FalseValue=Hidden}}" 
            Background="{DynamicResource Button1Background}"/>
        <Button
            Grid.Column="1" Grid.Row="1"
            FontSize="25"
            Content="{localization:Loc MultiplayerControl_JoinButton}"
            Command="{Binding PasteAndJoinCommand}"
            Visibility="{Binding CanConnect, Converter={converters:BooleanMultiValueConverter TrueValue=Visible, FalseValue=Hidden}}" 
            Background="{DynamicResource Button2Background}"/>
        <Button
            Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
            FontSize="25"
            Content="{localization:Loc MultiplayerControl_StopButton}"
            Command="{Binding StopCommand}"
            Visibility="{Binding IsConnected, Converter={converters:BooleanMultiValueConverter TrueValue=Visible, FalseValue=Hidden}}" 
            Background="{DynamicResource Button5Background}"/>
    </Grid>
</UserControl>